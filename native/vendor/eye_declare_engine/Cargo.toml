# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
name = "eye_declare_engine"
version = "0.7.1"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Inline terminal rendering engine: frame diffing, ANSI escape generation, scrollback-aware output"
readme = false
license = "MIT"
repository = "https://github.com/atuinsh/eye-declare"

[features]
test-util = ["dep:vte"]

[lib]
name = "eye_declare_engine"
path = "src/lib.rs"

[[test]]
name = "commit_scrollback"
path = "tests/commit_scrollback.rs"

[[test]]
name = "resize_reflow"
path = "tests/resize_reflow.rs"

[[test]]
name = "test_terminal_behavior"
path = "tests/test_terminal_behavior.rs"

[dependencies.ratatui-core]
version = "0.1.2"

[dependencies.unicode-width]
version = "0.2"

[dependencies.vte]
version = "0.15.0"
optional = true

[dev-dependencies]
